# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/

# Snapshots + processed data committed (needed for Streamlit Cloud)
# data/raw/        ← committed (small, ~600KB total)
# data/processed/  ← committed (small, ~850KB total)
# data/snapshots/  ← committed (CSV, daily updates via GitHub Actions)

# Exclude only the large PNG (regenerated by pipeline)
data/processed/bubble_vs_crash.png

# Logs
logs/

# Env / secrets
.env
*.env

# macOS
.DS_Store

# Editor
.vscode/
.idea/
